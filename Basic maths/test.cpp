#include <bits/stdc++.h>
using namespace std;
int main( ){

// program to find power of number
int a,b;
cout << "Enter the number and power: ";
cin >> a >> b;
int ans = pow(a,b);
cout << "Answer is: " << ans;
}

